default_permissions = "rebase-workspace"

[permissions.rebase-workspace]
description = "Edit source and complete a rebase in an ephemeral credential-free checkout."
extends = ":workspace"

[permissions.rebase-workspace.filesystem.":workspace_roots"]
".git" = "write"
".agents" = "read"
".codex" = "read"
